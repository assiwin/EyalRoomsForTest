# דיבוג האפליקציה ב-Windows

## הכנה ראשונית

1. חלץ את כל קובצי ה-ZIP לתיקייה רגילה.
2. הפעל פעם אחת את `Start.cmd`. הפעולה מתקינה Python 3.12, יוצרת `.venv` ומתקינה את הספריות.
3. לאחר שהאפליקציה נפתחה, אפשר לסגור אותה ואת חלון שורת הפקודה.

## דיבוג באמצעות PyCharm — מומלץ

1. בחר `File > Open` ופתח את תיקיית הפרויקט שחולצה.
2. אם PyCharm מבקש מפרש Python, בחר את `.venv\\Scripts\\python.exe` שנוצר לאחר הפעלת `Start.cmd`.
3. בסרגל העליון בחר את תצורת ההרצה `Exam Room App - Debug` שכבר כלולה בפרויקט.
4. פתח את `app.py` או את `engine.py` ולחץ בשוליים משמאל למספר שורה כדי להוסיף נקודת עצירה.
5. לחץ על סמל החרק או על `Shift+F9`.

אם התצורה אינה מופיעה, צור Run/Debug Configuration מסוג Python: בחר את `app.py`, הגדר Working directory לתיקיית הפרויקט והוסף משתנה סביבה `EXAM_ROOM_DEBUG=1`.

## דיבוג באמצעות VS Code

1. התקן את VS Code ואת ההרחבה הרשמית `Python` של Microsoft.
2. בחר `File > Open Folder` ופתח את התיקייה `ExamRoomApp_Debug_Project`.
3. פתח את `app.py` או את `engine.py` ולחץ בשוליים משמאל למספר שורה כדי להוסיף נקודת עצירה.
4. לחץ `F5` ובחר `Exam Room App — Debug` אם תתבקש.
5. הדפדפן ייפתח אוטומטית. בצע העלאה וחישוב כרגיל.

במצב דיבוג יוצגו בטרמינל בקשות ה-HTTP ופירוט מלא של שגיאות Python. במצב הרגיל המשתמש עדיין מקבל הודעת שגיאה נקייה ללא פרטים טכניים.

## נקודות עצירה שימושיות

- `app.py` בפונקציה `do_POST`: קליטת קובץ והפעלת החישוב.
- `app.py` בפונקציה `work`: כניסה למנוע השיבוץ.
- `engine.py` בפונקציה `read_book`: קריאת גיליון `dbase`.
- `engine.py` בפונקציה `solve`: בניית ופתרון מודל השיבוץ.
- `engine.py` בפונקציה `write_book`: כתיבת מספרי החדרים לעמודה I.

## הרצה ידנית מהטרמינל

```powershell
$env:EXAM_ROOM_DEBUG="1"
.\.venv\Scripts\python.exe .\app.py
```

עצירה: לחץ `Ctrl+C` בטרמינל.

## מבנה הפרויקט

- `app.py` — השרת המקומי וה-API.
- `engine.py` — קריאת Excel, אילוצי השיבוץ והאופטימיזציה.
- `web/` — ממשק המשתמש בעברית.
- `requirements.txt` — גרסאות NumPy ו-SciPy.
- `Start.cmd` — התקנה והרצה רגילה ב-Windows.
- `Build_EXE.cmd` — בניית גרסת EXE לאחר שהפרויקט נבדק.

קובצי תלמידים אינם כלולים בחבילת הפרויקט.
